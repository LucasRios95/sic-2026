# Dependências
node_modules/

# Build / output
dist/
dist-ssr/
build/
coverage/

# Variáveis de ambiente
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Certificados (NUNCA commitar)
*.pem
*.pfx
*.p12
*.key

# Editores / OS
.DS_Store
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
Thumbs.db

# Temporários
tmp/
.vite/
*.local

# Claude Code (local apenas)
.claude/settings.local.json

# Bundles de certificado ICP-Brasil — host-only
certs/*.pem
certs/*.crt
certs/*.der

# Artefatos de build / releases (nunca commitar - excedem limite do GitHub)
releases/
