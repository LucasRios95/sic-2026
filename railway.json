{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "npx tsx ./node_modules/typeorm/cli.js -d src/shared/infra/typeorm/data-source.ts migration:run && npx tsx src/shared/infra/typeorm/seeds/run-seeds.ts",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
